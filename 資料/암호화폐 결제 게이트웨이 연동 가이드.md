# 암호화폐 결제 게이트웨이 연동 가이드

## 개요
기존 서비스에 암호화폐 결제 옵션을 추가하는 것 - **서드파티 결제 게이트웨이 API 연동**

## 1. 주요 암호화폐 결제 게이트웨이 서비스

### 1.1 CoinPayments
- **특징**
  - 2,000+ 암호화폐 지원
  - 구독/반복 결제 기능
  - IPN (Instant Payment Notification)
  - API 문서 잘 되어있음
  
- **수수료**
  - 거래당 0.5%
  - 최소 수수료 없음
  
- **연동 난이도**: ⭐⭐ (쉬움)
- **공식 사이트**: https://www.coinpayments.net/
- **성인 콘텐츠**: ✅ 허용

### 1.2 NOWPayments
- **특징**
  - 150+ 암호화폐 지원
  - 자동 환전 기능 (암호화폐 → 법정화폐)
  - 간단한 API
  - 정기 결제 지원
  
- **수수료**
  - 거래당 0.5% - 1%
  - KYC 레벨에 따라 차등
  
- **연동 난이도**: ⭐ (매우 쉬움)
- **공식 사이트**: https://nowpayments.io/
- **성인 콘텐츠**: ✅ 허용 (확인 필요)

### 1.3 BTCPay Server
- **특징**
  - 완전 무료 (오픈소스)
  - 수수료 없음 (네트워크 수수료만)
  - 자체 호스팅 필요
  - 중개자 없음 → 완전한 통제권
  
- **수수료**
  - 0% (서버 운영비용만)
  
- **연동 난이도**: ⭐⭐⭐⭐ (복잡 - 서버 구축 필요)
- **공식 사이트**: https://btcpayserver.org/
- **성인 콘텐츠**: ✅ 허용 (자체 운영)

### 1.4 CoinGate
- **특징**
  - 70+ 암호화폐 지원
  - Lightning Network 지원
  - 유럽 기반
  - 자동 환전 가능
  
- **수수료**
  - 거래당 1%
  
- **연동 난이도**: ⭐⭐ (쉬움)
- **공식 사이트**: https://coingate.com/
- **성인 콘텐츠**: ⚠️ 확인 필요

### 1.5 Coinbase Commerce
- **특징**
  - 신뢰도 높음 (Coinbase 운영)
  - 주요 암호화폐만 지원 (BTC, ETH, USDC 등)
  - 직관적인 대시보드
  
- **수수료**
  - 거래당 1%
  
- **연동 난이도**: ⭐ (매우 쉬움)
- **공식 사이트**: https://commerce.coinbase.com/
- **성인 콘텐츠**: ❌ 제한 가능성 높음

## 2. 권장 서비스 (성인 콘텐츠용)

### 🥇 1순위: NOWPayments
**이유:**
- API 연동이 가장 간단
- 성인 콘텐츠 제약이 적음
- 한국어 지원
- 합리적인 수수료
- 자동 환전 가능 (암호화폐 보유 리스크 제거)

### 🥈 2순위: CoinPayments
**이유:**
- 가장 많은 암호화폐 지원
- 구독/반복 결제 특화
- 오래된 서비스 (신뢰도)

### 🥉 3순위: BTCPay Server (기술력 있을 경우)
**이유:**
- 완전 무료
- 수수료 0%
- 검열 저항성 완벽
- 자체 호스팅 필요 (VPS 비용만)

## 3. 연동 방법 (NOWPayments 기준)

### 3.1 계정 생성
1. NOWPayments 웹사이트 방문
2. 회원가입
3. KYC 진행 (간단 - 사업자 정보 제출)
4. API Key 발급

### 3.2 API 연동 (기본 플로우)

```javascript
// 1. 결제 생성 (백엔드)
const createPayment = async (amount, orderId) => {
  const response = await fetch('https://api.nowpayments.io/v1/payment', {
    method: 'POST',
    headers: {
      'x-api-key': 'YOUR_API_KEY',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      price_amount: amount,
      price_currency: 'usd',
      pay_currency: 'btc', // 또는 사용자가 선택한 암호화폐
      order_id: orderId,
      order_description: '챗봇 구독권',
      ipn_callback_url: 'https://yoursite.com/webhook/nowpayments'
    })
  });
  
  return await response.json();
};

// 2. 결제 URL로 리디렉션
// response.invoice_url로 사용자를 보냄

// 3. Webhook으로 결제 상태 수신 (백엔드)
app.post('/webhook/nowpayments', (req, res) => {
  const { payment_status, order_id } = req.body;
  
  if (payment_status === 'finished') {
    // 결제 완료 처리
    activateSubscription(order_id);
  }
  
  res.status(200).send('OK');
});
```

### 3.3 프론트엔드 통합

```javascript
// 사용자가 암호화폐 결제 선택
const handleCryptoPayment = async () => {
  // 1. 백엔드에 결제 생성 요청
  const payment = await fetch('/api/payment/crypto', {
    method: 'POST',
    body: JSON.stringify({
      amount: 29.99,
      product: 'monthly_subscription'
    })
  }).then(r => r.json());
  
  // 2. NOWPayments 결제 페이지로 리디렉션
  window.location.href = payment.invoice_url;
};
```

## 4. 실제 필요한 것들

### 4.1 개발 작업
- [ ] 결제 게이트웨이 API 연동 코드 작성 (1-2일)
- [ ] Webhook 엔드포인트 구현 (1일)
- [ ] 프론트엔드 결제 UI 추가 (1일)
- [ ] 테스트넷에서 테스트 (1일)

**총 개발 시간: 약 4-5일**

### 4.2 비용
- **개발 비용**: 거의 없음 (기존 개발자가 작업)
- **계정 등록 비용**: 무료
- **월 구독료**: 무료 (거래당 수수료만)
- **거래 수수료**: 0.5% - 1%

**초기 비용: $0 ~ 거의 없음**

### 4.3 KYC
- **판매자(당신)**: 간단한 사업자 KYC만 (게이트웨이에)
  - 사업자 정보
  - 이메일 인증
  - 신원 확인 (신분증)
  
- **구매자(사용자)**: KYC 불필요!
  - 암호화폐 지갑만 있으면 됨
  - 익명 결제 가능

### 4.4 일본 규제
- **당신(판매자) 입장**: 
  - ✅ 결제 게이트웨이가 라이선스 보유
  - ✅ 당신은 암호화폐 교환업자가 아님
  - ✅ 단순히 결제 수단을 제공하는 것뿐
  - ⚠️ 세금 신고는 해야 함 (일반 매출로)

## 5. 비교표

| 항목 | 직접 구축 | 게이트웨이 사용 |
|------|----------|----------------|
| 초기 비용 | $50,000+ | **$0** |
| 개발 기간 | 3-6개월 | **3-5일** |
| 라이선스 | 필요 (매우 비쌈) | **불필요** |
| KYC 시스템 | 구축 필요 | **게이트웨이가 처리** |
| 보안 책임 | 전체 | **게이트웨이가 처리** |
| 유지보수 | 지속적 필요 | **최소** |
| 거래 수수료 | 낮음 | 0.5-1% |
| 사용자 KYC | 필요 | **불필요** |

## 6. 구현 예시 (Node.js + Express)

### 6.1 백엔드 (server.js)

```javascript
const express = require('express');
const axios = require('axios');
const app = express();

const NOWPAYMENTS_API_KEY = process.env.NOWPAYMENTS_API_KEY;
const NOWPAYMENTS_IPN_SECRET = process.env.NOWPAYMENTS_IPN_SECRET;

// 결제 생성 엔드포인트
app.post('/api/payment/create', async (req, res) => {
  const { userId, planId, amount } = req.body;
  
  try {
    const payment = await axios.post(
      'https://api.nowpayments.io/v1/payment',
      {
        price_amount: amount,
        price_currency: 'usd',
        pay_currency: 'btc', // 사용자가 선택하게 할 수도 있음
        order_id: `user_${userId}_plan_${planId}_${Date.now()}`,
        order_description: '나모스 챗봇 구독권',
        ipn_callback_url: 'https://yoursite.com/api/payment/webhook',
        success_url: 'https://yoursite.com/payment/success',
        cancel_url: 'https://yoursite.com/payment/cancel'
      },
      {
        headers: {
          'x-api-key': NOWPAYMENTS_API_KEY,
          'Content-Type': 'application/json'
        }
      }
    );
    
    res.json({
      paymentUrl: payment.data.invoice_url,
      paymentId: payment.data.payment_id
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// Webhook 엔드포인트 (결제 완료 알림 수신)
app.post('/api/payment/webhook', express.raw({type: 'application/json'}), async (req, res) => {
  const payload = req.body;
  
  // 서명 검증 (보안)
  const signature = req.headers['x-nowpayments-sig'];
  // ... 서명 검증 로직
  
  const { payment_status, order_id, payment_id } = JSON.parse(payload);
  
  if (payment_status === 'finished') {
    // 결제 완료! 구독 활성화
    const userId = extractUserIdFromOrderId(order_id);
    await activateUserSubscription(userId);
    
    console.log(`✅ 결제 완료: ${order_id}`);
  } else if (payment_status === 'failed') {
    console.log(`❌ 결제 실패: ${order_id}`);
  }
  
  res.status(200).send('OK');
});

app.listen(3000);
```

### 6.2 프론트엔드 (React)

```jsx
import React, { useState } from 'react';

const CryptoPaymentButton = ({ userId, planId, amount }) => {
  const [loading, setLoading] = useState(false);
  
  const handlePayment = async () => {
    setLoading(true);
    
    try {
      const response = await fetch('/api/payment/create', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ userId, planId, amount })
      });
      
      const { paymentUrl } = await response.json();
      
      // NOWPayments 결제 페이지로 이동
      window.location.href = paymentUrl;
    } catch (error) {
      alert('결제 생성 실패: ' + error.message);
      setLoading(false);
    }
  };
  
  return (
    <button 
      onClick={handlePayment} 
      disabled={loading}
      className="crypto-payment-btn"
    >
      {loading ? '처리 중...' : '암호화폐로 결제하기 💰'}
    </button>
  );
};

export default CryptoPaymentButton;
```

## 7. 지원 암호화폐 선택 전략

### 7.1 필수 코인 (반드시 지원)
- **Bitcoin (BTC)**: 가장 보편적, 신뢰도 높음
- **USDT (Tether)**: 스테이블코인, 가격 안정적
- **USDC**: 스테이블코인, 규제 준수

### 7.2 추가 고려
- **Ethereum (ETH)**: 두 번째로 인기
- **Litecoin (LTC)**: 빠른 거래 속도
- **Monero (XMR)**: 프라이버시 특화 (성인 콘텐츠 사용자 선호)

### 7.3 권장 구성
```javascript
const supportedCryptos = [
  'btc',    // 필수
  'usdt',   // 필수 (안정성)
  'eth',    // 권장
  'ltc',    // 권장
  'xmr'     // 옵션 (프라이버시)
];
```

## 8. 가격 변동성 대응

### 8.1 문제
암호화폐는 가격 변동이 심함 → 받는 금액이 달라질 수 있음

### 8.2 해결책

#### 옵션 1: 스테이블코인 위주 (추천)
```javascript
// USDT, USDC만 받기
const acceptedCoins = ['usdt', 'usdc'];
```

#### 옵션 2: 즉시 환전
```javascript
// NOWPayments 설정에서 활성화
{
  "auto_conversion": true,
  "target_currency": "usd" // 또는 jpy
}
```
→ 암호화폐로 받아서 즉시 법정화폐로 환전

#### 옵션 3: 가격 고정 시간 설정
```javascript
{
  "fixed_rate": true,  // 가격 고정
  "lock_time": 900     // 15분간 고정
}
```

## 9. 실제 구현 체크리스트

### 단계 1: 준비 (1일)
- [ ] NOWPayments 또는 CoinPayments 계정 생성
- [ ] KYC 완료 (사업자 정보 제출)
- [ ] API Key 발급
- [ ] 지원할 암호화폐 선택

### 단계 2: 개발 (2-3일)
- [ ] 백엔드 API 엔드포인트 작성
  - [ ] 결제 생성 API
  - [ ] Webhook 수신 API
- [ ] 프론트엔드 버튼/UI 추가
- [ ] 데이터베이스 스키마 수정 (결제 이력)

### 단계 3: 테스트 (1일)
- [ ] Testnet/Sandbox 환경에서 테스트
- [ ] 결제 플로우 확인
- [ ] Webhook 정상 동작 확인
- [ ] 에러 핸들링 테스트

### 단계 4: 배포 (1일)
- [ ] Production API Key 적용
- [ ] 환경 변수 설정
- [ ] 모니터링 설정
- [ ] 사용자 공지

### 총 소요 시간: **5-6일**

## 10. 예상 수익률

### 10.1 수수료 비교
| 결제 수단 | 수수료 | 정산 주기 |
|----------|--------|----------|
| 신용카드 | 2.5-3.5% | 익일-익주 |
| PayPal | 3.5-4% | 즉시 |
| **암호화폐** | **0.5-1%** | **즉시** |

### 10.2 예상 절감액
월 매출 $10,000 기준:
- 신용카드 수수료: $300-350
- **암호화폐 수수료: $50-100**
- **절감액: $200-300/월**

## 11. 주의사항

### 11.1 세금
- 암호화폐로 받은 매출도 과세 대상
- 일본 세무서에 신고 필요
- 회계사와 상담 권장

### 11.2 환불 정책
- 블록체인 거래는 되돌릴 수 없음
- 환불 시 별도로 암호화폐를 보내야 함
- 환불 정책 명확히 공지 필요

### 11.3 고객 지원
- 암호화폐 초보 사용자를 위한 가이드 준비
- "지갑이 뭐예요?" 같은 질문 대비
- FAQ 작성 권장

## 12. 추가 리소스

### 12.1 공식 문서
- NOWPayments API: https://documenter.getpostman.com/view/7907941/S1a32n38
- CoinPayments API: https://www.coinpayments.net/merchant-tools-api
- BTCPay Server Docs: https://docs.btcpayserver.org/

### 12.2 라이브러리
- Node.js: `nowpayments-api-js`
- Python: `nowpayments-python`
- PHP: NOWPayments 공식 PHP SDK

### 12.3 테스트 도구
- Bitcoin Testnet Faucet: https://testnet-faucet.mempool.co/
- Ethereum Testnet: Sepolia, Goerli

## 13. 결론

### ✅ 해야 할 것
1. NOWPayments 계정 만들기
2. 간단한 KYC (사업자 정보)
3. API 연동 코드 작성 (4-5일)
4. 테스트 후 배포

### ❌ 하지 않아도 되는 것
1. ~~암호화폐 거래소 구축~~
2. ~~라이선스 취득~~
3. ~~사용자 KYC 시스템~~
4. ~~대규모 인프라 구축~~
5. ~~수십만 달러 투자~~

### 💰 실제 비용
- **초기 비용: $0**
- **개발 시간: 4-5일**
- **거래 수수료: 0.5-1%**
- **월 유지비: $0**

**→ 그냥 API 연동만 하면 끝!**


t
